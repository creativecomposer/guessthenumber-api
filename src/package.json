{
  "name": "Guess the Number game host service",
  "version": "0.0.1",
  "description": "",
  "main": "server.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "nodemon server.ts",
    "start": "NODE_ENV=\"production\" node ./bin/www",
    "test": "jest",
    "test:watch": "jest --watch",
    "prepare": "cd .. && node -e \"try { require('husky').install('src/.husky') } catch (e) {if (e.code !== 'MODULE_NOT_FOUND') throw e}\""
  },
  "keywords": [],
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "express-validator": "^7.0.1",
    "morgan": "^1.10.0",
    "nanoid": "^3.3.4"
  },
  "devDependencies": {
    "@types/cors": "^2.8.13",
    "@types/express": "^4.17.17",
    "@types/jest": "^29.5.1",
    "@types/morgan": "^1.9.4",
    "@types/node": "^20.2.1",
    "husky": "^8.0.3",
    "jest": "^29.5.0",
    "nodemon": "^2.0.22",
    "ts-jest": "^29.1.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.4"
  }
}
